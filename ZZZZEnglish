local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")

local player = Players.LocalPlayer

-- Settings
local settings = {
    auto = false,
    esp = false,
    spin = false,
    resetP = false,
    jump = false,
    hit = false,
    speed = false
}

-- Save key
local SAVE_KEY = "PvpLab_Saves"

-- Load settings
local function loadSettings()
    local data = player:GetAttribute(SAVE_KEY)
    if data then
        -- Parse manually
        settings.auto = string.find(data, '"auto":true') and true or false
        settings.esp = string.find(data, '"esp":true') and true or false
        settings.spin = string.find(data, '"spin":true') and true or false
        settings.resetP = string.find(data, '"resetP":true') and true or false
        settings.jump = string.find(data, '"jump":true') and true or false
        settings.hit = string.find(data, '"hit":true') and true or false
        settings.speed = string.find(data, '"speed":true') and true or false
        print("✅ Loaded: Auto="..tostring(settings.auto).." ESP="..tostring(settings.esp))
    end
end

-- Save settings
local function saveSettings()
    local json = '{"auto":'..tostring(settings.auto)..
                 ',"esp":'..tostring(settings.esp)..
                 ',"spin":'..tostring(settings.spin)..
                 ',"resetP":'..tostring(settings.resetP)..
                 ',"jump":'..tostring(settings.jump)..
                 ',"hit":'..tostring(settings.hit)..
                 ',"speed":'..tostring(settings.speed)..'}'
    player:SetAttribute(SAVE_KEY, json)
    print("💾 Saved!")
end

-- Create GUI
local gui = Instance.new("ScreenGui")
gui.Name = "PvpLab"
gui.ResetOnSpawn = false
gui.Parent = player:WaitForChild("PlayerGui")

-- Button to open
local openBtn = Instance.new("TextButton", gui)
openBtn.Size = UDim2.new(0,50,0,50)
openBtn.Position = UDim2.new(0,15,0.5,0)
openBtn.BackgroundColor3 = Color3.fromRGB(30,30,30)
openBtn.Text = "Z"
openBtn.Font = Enum.Font.GothamBlack
openBtn.TextSize = 24
openBtn.TextColor3 = Color3.new(1,1,1)
openBtn.Draggable = true
Instance.new("UICorner", openBtn).CornerRadius = UDim.new(0,10)

-- Main frame
local frame = Instance.new("Frame", gui)
frame.Size = UDim2.new(0,280,0,400)
frame.Position = UDim2.new(0.5,-140,0.5,-200)
frame.BackgroundColor3 = Color3.fromRGB(20,20,20)
frame.Visible = false
frame.Draggable = true
Instance.new("UICorner", frame).CornerRadius = UDim.new(0,12)

-- Title
local title = Instance.new("TextLabel", frame)
title.Size = UDim2.new(1,0,0,40)
title.Text = "PVP LAB"
title.Font = Enum.Font.GothamBold
title.TextSize = 18
title.TextColor3 = Color3.new(1,1,1)
title.BackgroundTransparency = 1

-- Close button
local closeBtn = Instance.new("TextButton", frame)
closeBtn.Size = UDim2.new(0,30,0,30)
closeBtn.Position = UDim2.new(1,-35,0,5)
closeBtn.Text = "X"
closeBtn.BackgroundTransparency = 1
closeBtn.TextColor3 = Color3.new(1,1,1)
closeBtn.TextSize = 18

-- Scroll frame for buttons
local scroll = Instance.new("ScrollingFrame", frame)
scroll.Size = UDim2.new(1,0,1,-50)
scroll.Position = UDim2.new(0,0,0,45)
scroll.BackgroundTransparency = 1
scroll.CanvasSize = UDim2.new(0,0,0,350)
scroll.ScrollBarThickness = 5

-- Store buttons
local buttons = {}

-- Function to create toggle button
local function createToggle(text, y, key)
    local btn = Instance.new("TextButton", scroll)
    btn.Size = UDim2.new(0.9,0,0,40)
    btn.Position = UDim2.new(0.05,0,0,y)
    btn.Text = text .. " [OFF]"
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 14
    btn.BackgroundColor3 = Color3.fromRGB(50,50,50)
    btn.TextColor3 = Color3.new(1,1,1)
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0,8)
    
    -- Update text based on setting
    local function updateButton()
        if settings[key] then
            btn.Text = text .. " [ON]"
            btn.BackgroundColor3 = Color3.fromRGB(0,150,0)
        else
            btn.Text = text .. " [OFF]"
            btn.BackgroundColor3 = Color3.fromRGB(50,50,50)
        end
    end
    
    updateButton()
    
    btn.MouseButton1Click:Connect(function()
        settings[key] = not settings[key]
        updateButton()
        saveSettings()
        print(text .. " is now " .. tostring(settings[key]))
    end)
    
    buttons[key] = btn
    return btn
end

-- Create all buttons
createToggle("Auto Attack", 10, "auto")
createToggle("ESP", 60, "esp")
createToggle("Spin Bot", 110, "spin")
createToggle("Reset Player", 160, "resetP")
createToggle("Super Jump", 210, "jump")
createToggle("Auto Hit", 260, "hit")
createToggle("Speed", 310, "speed")

-- Credits tab button
local creditsBtn = Instance.new("TextButton", frame)
creditsBtn.Size = UDim2.new(0.4,0,0,30)
creditsBtn.Position = UDim2.new(0.55,0,0,5)
creditsBtn.Text = "CREDITS"
creditsBtn.Font = Enum.Font.GothamBold
creditsBtn.TextSize = 12
creditsBtn.BackgroundColor3 = Color3.fromRGB(80,80,80)
creditsBtn.TextColor3 = Color3.new(1,1,1)
Instance.new("UICorner", creditsBtn).CornerRadius = UDim.new(0,6)

-- Credits frame
local creditsFrame = Instance.new("Frame", frame)
creditsFrame.Size = UDim2.new(1,0,1,-50)
creditsFrame.Position = UDim2.new(0,0,0,45)
creditsFrame.BackgroundColor3 = Color3.fromRGB(25,25,25)
creditsFrame.Visible = false
Instance.new("UICorner", creditsFrame).CornerRadius = UDim.new(0,8)

-- Credits content
local dev1 = Instance.new("TextLabel", creditsFrame)
dev1.Size = UDim2.new(1,0,0,40)
dev1.Position = UDim2.new(0,0,0,20)
dev1.Text = "⭐ ZZZ\nLead Developer"
dev1.Font = Enum.Font.GothamBold
dev1.TextSize = 12
dev1.TextColor3 = Color3.new(1,1,1)
dev1.BackgroundTransparency = 1

local dev2 = Instance.new("TextLabel", creditsFrame)
dev2.Size = UDim2.new(1,0,0,40)
dev2.Position = UDim2.new(0,0,0,70)
dev2.Text = "⚡ mathyas\nCo-Developer"
dev2.Font = Enum.Font.GothamBold
dev2.TextSize = 12
dev2.TextColor3 = Color3.new(1,1,1)
dev2.BackgroundTransparency = 1

local discordBtn = Instance.new("TextButton", creditsFrame)
discordBtn.Size = UDim2.new(0.8,0,0,50)
discordBtn.Position = UDim2.new(0.1,0,0,130)
discordBtn.Text = "💬 JOIN DISCORD\nclick to copy link"
discordBtn.Font = Enum.Font.GothamBold
discordBtn.TextSize = 12
discordBtn.BackgroundColor3 = Color3.fromRGB(88,101,242)
discordBtn.TextColor3 = Color3.new(1,1,1)
Instance.new("UICorner", discordBtn).CornerRadius = UDim.new(0,8)

discordBtn.MouseButton1Click:Connect(function()
    local link = "https://discord.gg/bBWPETWW"
    if setclipboard then setclipboard(link) end
    local notif = Instance.new("TextLabel", frame)
    notif.Size = UDim2.new(0,200,0,30)
    notif.Position = UDim2.new(0.5,-100,0.5,150)
    notif.Text = "Link copied!"
    notif.BackgroundColor3 = Color3.fromRGB(0,170,0)
    notif.TextColor3 = Color3.new(1,1,1)
    notif.Font = Enum.Font.GothamBold
    notif.TextSize = 12
    Instance.new("UICorner", notif).CornerRadius = UDim.new(0,5)
    task.wait(2)
    notif:Destroy()
end)

local version = Instance.new("TextLabel", creditsFrame)
version.Size = UDim2.new(1,0,0,30)
version.Position = UDim2.new(0,0,0,200)
version.Text = "📌 Version 2.0"
version.Font = Enum.Font.Gotham
version.TextSize = 10
version.TextColor3 = Color3.fromRGB(150,150,150)
version.BackgroundTransparency = 1

local thanks = Instance.new("TextLabel", creditsFrame)
thanks.Size = UDim2.new(1,0,0,30)
thanks.Position = UDim2.new(0,0,0,230)
thanks.Text = "❤️ Thanks for using!"
thanks.Font = Enum.Font.Gotham
thanks.TextSize = 10
thanks.TextColor3 = Color3.fromRGB(200,200,200)
thanks.BackgroundTransparency = 1

-- Tab switching
local showingCredits = false
creditsBtn.MouseButton1Click:Connect(function()
    showingCredits = not showingCredits
    scroll.Visible = not showingCredits
    creditsFrame.Visible = showingCredits
    if showingCredits then
        creditsBtn.BackgroundColor3 = Color3.fromRGB(60,60,60)
    else
        creditsBtn.BackgroundColor3 = Color3.fromRGB(80,80,80)
    end
end)

openBtn.MouseButton1Click:Connect(function()
    frame.Visible = not frame.Visible
end)

closeBtn.MouseButton1Click:Connect(function()
    frame.Visible = false
end)

-- Load settings
loadSettings()

-- Update all buttons after loading
task.wait(0.5)
for key, btn in pairs(buttons) do
    if settings[key] then
        btn.Text = btn.Text:gsub("%[OFF%]", "[ON]")
        btn.Text = btn.Text:gsub("%[ON%]", "[ON]")
        btn.BackgroundColor3 = Color3.fromRGB(0,150,0)
    else
        btn.Text = btn.Text:gsub("%[ON%]", "[OFF]")
        btn.Text = btn.Text:gsub("%[OFF%]", "[OFF]")
        btn.BackgroundColor3 = Color3.fromRGB(50,50,50)
    end
end

-- FUNCTIONS

-- Auto Attack
RunService.RenderStepped:Connect(function()
    if settings.auto and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
        local hrp = player.Character.HumanoidRootPart
        local closest = nil
        local dist = math.huge
        
        for _, v in pairs(Players:GetPlayers()) do
            if v ~= player and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
                local d = (v.Character.HumanoidRootPart.Position - hrp.Position).Magnitude
                if d < dist then
                    dist = d
                    closest = v
                end
            end
        end
        
        if closest then
            hrp.Velocity = (closest.Character.HumanoidRootPart.Position - hrp.Position).Unit * 60
        end
    end
end)

-- Auto Hit
RunService.RenderStepped:Connect(function()
    if settings.hit and player.Character then
        local hum = player.Character:FindFirstChildOfClass("Humanoid")
        local hrp = player.Character:FindFirstChild("HumanoidRootPart")
        
        if hum and hrp then
            local closest = nil
            local dist = math.huge
            
            for _, v in pairs(Players:GetPlayers()) do
                if v ~= player and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
                    local d = (v.Character.HumanoidRootPart.Position - hrp.Position).Magnitude
                    if d < dist and d < 10 then
                        dist = d
                        closest = v
                    end
                end
            end
            
            if closest and closest.Character then
                hum:MoveTo(closest.Character.HumanoidRootPart.Position)
                local tool = player.Character:FindFirstChildOfClass("Tool")
                if tool then tool:Activate() end
            end
        end
    end
end)

-- Speed
local speedActive = false
local function setSpeed()
    if not player.Character then return end
    local hum = player.Character:FindFirstChildOfClass("Humanoid")
    if hum then
        hum.WalkSpeed = speedActive and 50 or 27
    end
end

spawn(function()
    while true do
        task.wait(0.1)
        if speedActive ~= settings.speed then
            speedActive = settings.speed
            setSpeed()
        end
        setSpeed()
    end
end)

player.CharacterAdded:Connect(function()
    task.wait(0.2)
    setSpeed()
end)

-- Spin Bot
RunService.RenderStepped:Connect(function()
    if settings.spin and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
        player.Character.HumanoidRootPart.CFrame = player.Character.HumanoidRootPart.CFrame * CFrame.Angles(0, math.rad(25), 0)
    end
end)

-- Reset Player
local resetCooldown = false
RunService.RenderStepped:Connect(function()
    if settings.resetP and not resetCooldown then
        resetCooldown = true
        if player.Character and player.Character:FindFirstChildOfClass("Humanoid") then
            player.Character:FindFirstChildOfClass("Humanoid").Health = 0
        end
        task.wait(1)
        resetCooldown = false
    end
end)

-- Super Jump
local jumpTime = 0
UserInputService.InputBegan:Connect(function(input, processed)
    if processed then return end
    if settings.jump and input.KeyCode == Enum.KeyCode.Space then
        local hum = player.Character and player.Character:FindFirstChildOfClass("Humanoid")
        if hum and tick() - jumpTime > 0.1 then
            jumpTime = tick()
            hum:ChangeState(Enum.HumanoidStateType.Jumping)
            hum.JumpPower = 120
        end
    end
end)

RunService.RenderStepped:Connect(function()
    if player.Character then
        local hum = player.Character:FindFirstChildOfClass("Humanoid")
        if hum then
            hum.JumpPower = settings.jump and 120 or 50
        end
    end
end)

-- ESP
RunService.RenderStepped:Connect(function()
    if settings.esp then
        for _, v in pairs(Players:GetPlayers()) do
            if v ~= player and v.Character then
                if not v.Character:FindFirstChild("ESP_Box") then
                    local box = Instance.new("BoxHandleAdornment")
                    box.Name = "ESP_Box"
                    box.Size = Vector3.new(4, 5, 4)
                    box.Color3 = Color3.fromRGB(255,0,0)
                    box.AlwaysOnTop = true
                    box.ZIndex = 10
                    box.Adornee = v.Character:FindFirstChild("HumanoidRootPart")
                    box.Parent = v.Character
                end
            end
        end
    else
        for _, v in pairs(Players:GetPlayers()) do
            if v.Character then
                local box = v.Character:FindFirstChild("ESP_Box")
                if box then box:Destroy() end
            end
        end
    end
end)

print("✅ Script loaded!")
print("💾 Settings auto-save when you click buttons!")
print("📌 Press Z to open menu")
