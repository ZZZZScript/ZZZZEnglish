local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TeleportService = game:GetService("TeleportService")
local TweenService = game:GetService("TweenService")

local player = Players.LocalPlayer

-- Remover GUIs antigas
for _, gui in pairs(player.PlayerGui:GetChildren()) do
    if gui.Name == "ZzzUI" then
        gui:Destroy()
    end
end

-- Criar GUI
local gui = Instance.new("ScreenGui")
gui.Name = "ZzzUI"
gui.ResetOnSpawn = false
gui.Parent = player:WaitForChild("PlayerGui")

-- CORES
local corFundo = Color3.fromRGB(20, 22, 30)
local corCard = Color3.fromRGB(30, 32, 42)
local corAzul = Color3.fromRGB(66, 133, 244)
local corRoxo = Color3.fromRGB(156, 39, 176)
local corTextoBranco = Color3.fromRGB(255, 255, 255)
local corTextoClaro = Color3.fromRGB(180, 185, 200)
local corPreto = Color3.fromRGB(25, 25, 35)

-- ========== ÍCONE FLUTUANTE (canto esquerdo) ==========
local iconButton = Instance.new("TextButton")
iconButton.Size = UDim2.new(0, 55, 0, 55)
iconButton.Position = UDim2.new(0, 12, 0.5, -27)
iconButton.BackgroundColor3 = corPreto
iconButton.Text = "Z"
iconButton.TextColor3 = corTextoBranco
iconButton.TextScaled = true
iconButton.Font = Enum.Font.GothamBlack
iconButton.Parent = gui
Instance.new("UICorner", iconButton).CornerRadius = UDim.new(0, 15)

-- ========== MENU PRINCIPAL ==========
local menu = Instance.new("Frame")
menu.Size = UDim2.new(0, 420, 0, 550)
menu.Position = UDim2.new(0.5, -210, 0.5, -275)
menu.BackgroundColor3 = corFundo
menu.BackgroundTransparency = 0
menu.Visible = false
menu.Parent = gui
Instance.new("UICorner", menu).CornerRadius = UDim.new(0, 20)

-- ========== TOPO PRETO (retângulo) ==========
local topoPreto = Instance.new("Frame")
topoPreto.Size = UDim2.new(1, 0, 0, 85)
topoPreto.Position = UDim2.new(0, 0, 0, 0)
topoPreto.BackgroundColor3 = corPreto
topoPreto.BackgroundTransparency = 0
topoPreto.Parent = menu
Instance.new("UICorner", topoPreto).CornerRadius = UDim.new(0, 20)

-- Título Zzz V1
local tituloZ = Instance.new("TextLabel")
tituloZ.Size = UDim2.new(1, 0, 0, 40)
tituloZ.Position = UDim2.new(0, 0, 0, 12)
tituloZ.Text = "Zzz V1"
tituloZ.Font = Enum.Font.GothamBlack
tituloZ.TextSize = 32
tituloZ.TextColor3 = corTextoBranco
tituloZ.BackgroundTransparency = 1
tituloZ.Parent = topoPreto

-- Developer text
local devText = Instance.new("TextLabel")
devText.Size = UDim2.new(1, 0, 0, 22)
devText.Position = UDim2.new(0, 0, 0, 52)
devText.Text = "developer Mathyas and Zzz"
devText.Font = Enum.Font.Gotham
devText.TextSize = 11
devText.TextColor3 = corTextoClaro
devText.BackgroundTransparency = 1
devText.Parent = topoPreto

-- Botão fechar
local fechar = Instance.new("TextButton")
fechar.Size = UDim2.new(0, 32, 0, 32)
fechar.Position = UDim2.new(1, -42, 0, 12)
fechar.Text = "✕"
fechar.Font = Enum.Font.GothamBold
fechar.TextSize = 18
fechar.TextColor3 = corTextoBranco
fechar.BackgroundTransparency = 1
fechar.Parent = topoPreto

-- ========== ABAS LADO ESQUERDO ==========
local sidebar = Instance.new("Frame")
sidebar.Size = UDim2.new(0, 100, 1, -85)
sidebar.Position = UDim2.new(0, 0, 0, 85)
sidebar.BackgroundColor3 = corCard
sidebar.BackgroundTransparency = 0
sidebar.Parent = menu

-- Aba Main
local abaMain = Instance.new("TextButton")
abaMain.Size = UDim2.new(0.85, 0, 0, 45)
abaMain.Position = UDim2.new(0.075, 0, 0, 15)
abaMain.Text = "Main"
abaMain.Font = Enum.Font.GothamBold
abaMain.TextSize = 14
abaMain.TextColor3 = corTextoBranco
abaMain.BackgroundColor3 = corAzul
abaMain.Parent = sidebar
Instance.new("UICorner", abaMain).CornerRadius = UDim.new(0, 8)

-- Aba Credits
local abaCredits = Instance.new("TextButton")
abaCredits.Size = UDim2.new(0.85, 0, 0, 45)
abaCredits.Position = UDim2.new(0.075, 0, 0, 70)
abaCredits.Text = "Credits"
abaCredits.Font = Enum.Font.GothamBold
abaCredits.TextSize = 14
abaCredits.TextColor3 = corTextoClaro
abaCredits.BackgroundColor3 = corCard
abaCredits.Parent = sidebar
Instance.new("UICorner", abaCredits).CornerRadius = UDim.new(0, 8)

-- ========== CONTEÚDO PRINCIPAL ==========
local contentArea = Instance.new("Frame")
contentArea.Size = UDim2.new(1, -110, 1, -95)
contentArea.Position = UDim2.new(0, 110, 0, 95)
contentArea.BackgroundTransparency = 1
contentArea.Parent = menu

-- Scroll para Main
local scrollMain = Instance.new("ScrollingFrame")
scrollMain.Size = UDim2.new(1, 0, 1, 0)
scrollMain.BackgroundTransparency = 1
scrollMain.CanvasSize = UDim2.new(0, 0, 0, 550)
scrollMain.ScrollBarThickness = 4
scrollMain.ScrollBarImageColor3 = corAzul
scrollMain.Parent = contentArea
scrollMain.Visible = true

-- Scroll para Credits
local scrollCredits = Instance.new("ScrollingFrame")
scrollCredits.Size = UDim2.new(1, 0, 1, 0)
scrollCredits.BackgroundTransparency = 1
scrollCredits.CanvasSize = UDim2.new(0, 0, 0, 400)
scrollCredits.ScrollBarThickness = 4
scrollCredits.ScrollBarImageColor3 = corAzul
scrollCredits.Parent = contentArea
scrollCredits.Visible = false

-- ========== FUNÇÃO CRIAR CARD ==========
local function criarCard(icone, texto, desc, parent, y)
    local card = Instance.new("Frame")
    card.Size = UDim2.new(1, 0, 0, 65)
    card.Position = UDim2.new(0, 0, 0, y)
    card.BackgroundColor3 = corCard
    card.Parent = parent
    Instance.new("UICorner", card).CornerRadius = UDim.new(0, 12)
    
    local iconeLab = Instance.new("TextLabel")
    iconeLab.Size = UDim2.new(0, 45, 0, 45)
    iconeLab.Position = UDim2.new(0, 12, 0.5, -22.5)
    iconeLab.Text = icone
    iconeLab.Font = Enum.Font.Gotham
    iconeLab.TextSize = 26
    iconeLab.TextColor3 = corAzul
    iconeLab.BackgroundTransparency = 1
    iconeLab.Parent = card
    
    local tituloCard = Instance.new("TextLabel")
    tituloCard.Size = UDim2.new(0, 150, 0, 22)
    tituloCard.Position = UDim2.new(0, 65, 0, 10)
    tituloCard.Text = texto
    tituloCard.Font = Enum.Font.GothamBold
    tituloCard.TextSize = 14
    tituloCard.TextColor3 = corTextoBranco
    tituloCard.BackgroundTransparency = 1
    tituloCard.TextXAlignment = Enum.TextXAlignment.Left
    tituloCard.Parent = card
    
    local descCard = Instance.new("TextLabel")
    descCard.Size = UDim2.new(0, 180, 0, 18)
    descCard.Position = UDim2.new(0, 65, 0, 34)
    descCard.Text = desc
    descCard.Font = Enum.Font.Gotham
    descCard.TextSize = 9
    descCard.TextColor3 = corTextoClaro
    descCard.BackgroundTransparency = 1
    descCard.TextXAlignment = Enum.TextXAlignment.Left
    descCard.Parent = card
    
    local toggle = Instance.new("TextButton")
    toggle.Size = UDim2.new(0, 52, 0, 28)
    toggle.Position = UDim2.new(1, -62, 0.5, -14)
    toggle.Text = ""
    toggle.BackgroundColor3 = Color3.fromRGB(55, 58, 70)
    toggle.Parent = card
    Instance.new("UICorner", toggle).CornerRadius = UDim.new(1, 0)
    
    local bola = Instance.new("Frame")
    bola.Size = UDim2.new(0, 22, 0, 22)
    bola.Position = UDim2.new(0, 4, 0.5, -11)
    bola.BackgroundColor3 = corTextoBranco
    bola.Parent = toggle
    Instance.new("UICorner", bola).CornerRadius = UDim.new(1, 0)
    
    local status = Instance.new("TextLabel")
    status.Size = UDim2.new(0, 40, 0, 20)
    status.Position = UDim2.new(1, -118, 0.5, -10)
    status.Text = "OFF"
    status.Font = Enum.Font.GothamBold
    status.TextSize = 9
    status.TextColor3 = Color3.fromRGB(255, 80, 80)
    status.BackgroundTransparency = 1
    status.Parent = card
    
    local ativo = false
    local callback = nil
    
    toggle.MouseButton1Click:Connect(function()
        ativo = not ativo
        if ativo then
            toggle.BackgroundColor3 = corAzul
            bola.Position = UDim2.new(1, -26, 0.5, -11)
            status.Text = "ON"
            status.TextColor3 = Color3.fromRGB(80, 255, 80)
        else
            toggle.BackgroundColor3 = Color3.fromRGB(55, 58, 70)
            bola.Position = UDim2.new(0, 4, 0.5, -11)
            status.Text = "OFF"
            status.TextColor3 = Color3.fromRGB(255, 80, 80)
        end
        if callback then callback(ativo) end
    end)
    
    return {
        onToggle = function(cb) callback = cb end
    }
end

-- ========== ABA MAIN ==========
local mainY = 10

-- Título PVP Laboratório
local pvpTitle = Instance.new("TextLabel")
pvpTitle.Size = UDim2.new(1, 0, 0, 35)
pvpTitle.Position = UDim2.new(0, 0, 0, mainY)
pvpTitle.Text = "⚔️ PVP LABORATÓRIO"
pvpTitle.Font = Enum.Font.GothamBold
pvpTitle.TextSize = 14
pvpTitle.TextColor3 = corAzul
pvpTitle.BackgroundTransparency = 1
pvpTitle.TextXAlignment = Enum.TextXAlignment.Left
pvpTitle.Parent = scrollMain
mainY = mainY + 42

-- Auto Attack
local autoAttack = criarCard("⚔️", "Auto Attack", "Auto attack enemies", scrollMain, mainY)
mainY = mainY + 72

-- ESP
local esp = criarCard("👁️", "ESP", "See enemies through walls", scrollMain, mainY)
mainY = mainY + 72

-- Spin Bot
local spinBot = criarCard("🌀", "Spin Bot", "Auto spin while moving", scrollMain, mainY)
mainY = mainY + 72

-- Super Jump
local superJump = criarCard("🦘", "Super Jump", "Jump much higher", scrollMain, mainY)
mainY = mainY + 72

-- Auto Hit
local autoHit = criarCard("👊", "Auto Hit", "Auto attack nearby", scrollMain, mainY)
mainY = mainY + 72

-- Speed
local speed = criarCard("🏃", "Speed", "Increase movement speed", scrollMain, mainY)
mainY = mainY + 80

-- Título Utility
local utilTitle = Instance.new("TextLabel")
utilTitle.Size = UDim2.new(1, 0, 0, 35)
utilTitle.Position = UDim2.new(0, 0, 0, mainY)
utilTitle.Text = "🔧 UTILITY"
utilTitle.Font = Enum.Font.GothamBold
utilTitle.TextSize = 14
utilTitle.TextColor3 = corAzul
utilTitle.BackgroundTransparency = 1
utilTitle.TextXAlignment = Enum.TextXAlignment.Left
utilTitle.Parent = scrollMain
mainY = mainY + 42

-- Auto Rejoin
local autoRejoinBtn = Instance.new("TextButton")
autoRejoinBtn.Size = UDim2.new(1, 0, 0, 55)
autoRejoinBtn.Position = UDim2.new(0, 0, 0, mainY)
autoRejoinBtn.Text = "🔄 Auto Rejoin"
autoRejoinBtn.Font = Enum.Font.GothamBold
autoRejoinBtn.TextSize = 13
autoRejoinBtn.TextColor3 = corTextoBranco
autoRejoinBtn.BackgroundColor3 = corCard
autoRejoinBtn.Parent = scrollMain
Instance.new("UICorner", autoRejoinBtn).CornerRadius = UDim.new(0, 12)

autoRejoinBtn.MouseButton1Click:Connect(function()
    TeleportService:Teleport(game.PlaceId, player)
end)

mainY = mainY + 65

scrollMain.CanvasSize = UDim2.new(0, 0, 0, mainY + 20)

-- ========== ABA CREDITS ==========
local creditsY = 10

local creditsTitle = Instance.new("TextLabel")
creditsTitle.Size = UDim2.new(1, 0, 0, 35)
creditsTitle.Position = UDim2.new(0, 0, 0, creditsY)
creditsTitle.Text = "👥 CREDITS"
creditsTitle.Font = Enum.Font.GothamBold
creditsTitle.TextSize = 14
creditsTitle.TextColor3 = corAzul
creditsTitle.BackgroundTransparency = 1
creditsTitle.TextXAlignment = Enum.TextXAlignment.Left
creditsTitle.Parent = scrollCredits
creditsY = creditsY + 42

-- Card Developer 1
local dev1Card = Instance.new("Frame")
dev1Card.Size = UDim2.new(1, 0, 0, 75)
dev1Card.Position = UDim2.new(0, 0, 0, creditsY)
dev1Card.BackgroundColor3 = corCard
dev1Card.Parent = scrollCredits
Instance.new("UICorner", dev1Card).CornerRadius = UDim.new(0, 12)

local dev1Icon = Instance.new("TextLabel")
dev1Icon.Size = UDim2.new(0, 50, 0, 50)
dev1Icon.Position = UDim2.new(0, 12, 0.5, -25)
dev1Icon.Text = "👨‍💻"
dev1Icon.Font = Enum.Font.Gotham
dev1Icon.TextSize = 32
dev1Icon.TextColor3 = corAzul
dev1Icon.BackgroundTransparency = 1
dev1Icon.Parent = dev1Card

local dev1Name = Instance.new("TextLabel")
dev1Name.Size = UDim2.new(0, 200, 0, 22)
dev1Name.Position = UDim2.new(0, 72, 0, 12)
dev1Name.Text = "Mathyas"
dev1Name.Font = Enum.Font.GothamBold
dev1Name.TextSize = 16
dev1Name.TextColor3 = corTextoBranco
dev1Name.BackgroundTransparency = 1
dev1Name.TextXAlignment = Enum.TextXAlignment.Left
dev1Name.Parent = dev1Card

local dev1Role = Instance.new("TextLabel")
dev1Role.Size = UDim2.new(0, 200, 0, 18)
dev1Role.Position = UDim2.new(0, 72, 0, 38)
dev1Role.Text = "Lead Developer & Creator"
dev1Role.Font = Enum.Font.Gotham
dev1Role.TextSize = 10
dev1Role.TextColor3 = corTextoClaro
dev1Role.BackgroundTransparency = 1
dev1Role.TextXAlignment = Enum.TextXAlignment.Left
dev1Role.Parent = dev1Card

creditsY = creditsY + 82

-- Card Developer 2
local dev2Card = Instance.new("Frame")
dev2Card.Size = UDim2.new(1, 0, 0, 75)
dev2Card.Position = UDim2.new(0, 0, 0, creditsY)
dev2Card.BackgroundColor3 = corCard
dev2Card.Parent = scrollCredits
Instance.new("UICorner", dev2Card).CornerRadius = UDim.new(0, 12)

local dev2Icon = Instance.new("TextLabel")
dev2Icon.Size = UDim2.new(0, 50, 0, 50)
dev2Icon.Position = UDim2.new(0, 12, 0.5, -25)
dev2Icon.Text = "👨‍💻"
dev2Icon.Font = Enum.Font.Gotham
dev2Icon.TextSize = 32
dev2Icon.TextColor3 = corAzul
dev2Icon.BackgroundTransparency = 1
dev2Icon.Parent = dev2Card

local dev2Name = Instance.new("TextLabel")
dev2Name.Size = UDim2.new(0, 200, 0, 22)
dev2Name.Position = UDim2.new(0, 72, 0, 12)
dev2Name.Text = "Zzz"
dev2Name.Font = Enum.Font.GothamBold
dev2Name.TextSize = 16
dev2Name.TextColor3 = corTextoBranco
dev2Name.BackgroundTransparency = 1
dev2Name.TextXAlignment = Enum.TextXAlignment.Left
dev2Name.Parent = dev2Card

local dev2Role = Instance.new("TextLabel")
dev2Role.Size = UDim2.new(0, 200, 0, 18)
dev2Role.Position = UDim2.new(0, 72, 0, 38)
dev2Role.Text = "Co-Developer & Programmer"
dev2Role.Font = Enum.Font.Gotham
dev2Role.TextSize = 10
dev2Role.TextColor3 = corTextoClaro
dev2Role.BackgroundTransparency = 1
dev2Role.TextXAlignment = Enum.TextXAlignment.Left
dev2Role.Parent = dev2Card

creditsY = creditsY + 82

-- Discord Button
local discordBtn = Instance.new("TextButton")
discordBtn.Size = UDim2.new(1, 0, 0, 55)
discordBtn.Position = UDim2.new(0, 0, 0, creditsY)
discordBtn.Text = "💬 JOIN OUR DISCORD"
discordBtn.Font = Enum.Font.GothamBold
discordBtn.TextSize = 13
discordBtn.TextColor3 = corTextoBranco
discordBtn.BackgroundColor3 = Color3.fromRGB(88, 101, 242)
discordBtn.Parent = scrollCredits
Instance.new("UICorner", discordBtn).CornerRadius = UDim.new(0, 12)

discordBtn.MouseButton1Click:Connect(function()
    setclipboard("https://discord.gg/PDmAh9Ua4")
    local notif = Instance.new("TextLabel")
    notif.Size = UDim2.new(0, 200, 0, 28)
    notif.Position = UDim2.new(0.5, -100, 0.5, 150)
    notif.Text = "✓ Discord link copied!"
    notif.BackgroundColor3 = corAzul
    notif.TextColor3 = corTextoBranco
    notif.Font = Enum.Font.GothamBold
    notif.TextSize = 11
    notif.Parent = menu
    Instance.new("UICorner", notif).CornerRadius = UDim.new(0, 8)
    task.wait(2)
    notif:Destroy()
end)

creditsY = creditsY + 65

-- Version
local versionLabel = Instance.new("TextLabel")
versionLabel.Size = UDim2.new(1, 0, 0, 25)
versionLabel.Position = UDim2.new(0, 0, 0, creditsY)
versionLabel.Text = "Zzz V1 | Premium Script"
versionLabel.Font = Enum.Font.Gotham
versionLabel.TextSize = 8
versionLabel.TextColor3 = Color3.fromRGB(100, 105, 120)
versionLabel.BackgroundTransparency = 1
versionLabel.Parent = scrollCredits

creditsY = creditsY + 35
scrollCredits.CanvasSize = UDim2.new(0, 0, 0, creditsY + 20)

-- ========== TROCAR ABA ==========
local function trocarAba(aba)
    if aba == "Main" then
        abaMain.BackgroundColor3 = corAzul
        abaMain.TextColor3 = corTextoBranco
        abaCredits.BackgroundColor3 = corCard
        abaCredits.TextColor3 = corTextoClaro
        scrollMain.Visible = true
        scrollCredits.Visible = false
    else
        abaCredits.BackgroundColor3 = corAzul
        abaCredits.TextColor3 = corTextoBranco
        abaMain.BackgroundColor3 = corCard
        abaMain.TextColor3 = corTextoClaro
        scrollMain.Visible = false
        scrollCredits.Visible = true
    end
end

abaMain.MouseButton1Click:Connect(function()
    trocarAba("Main")
end)

abaCredits.MouseButton1Click:Connect(function()
    trocarAba("Credits")
end)

-- ========== FUNÇÕES ==========
local active = {
    auto = false,
    esp = false,
    spin = false,
    superJump = false,
    hit = false,
    speed = false
}

autoAttack.onToggle(function(v) active.auto = v end)
esp.onToggle(function(v) active.esp = v end)
spinBot.onToggle(function(v) active.spin = v end)
superJump.onToggle(function(v) active.superJump = v end)
autoHit.onToggle(function(v) active.hit = v end)
speed.onToggle(function(v) active.speed = v end)

-- Super Jump
local function setSuperJump()
    if player.Character then
        local hum = player.Character:FindFirstChildOfClass("Humanoid")
        if hum then
            hum.JumpPower = active.superJump and 120 or 50
        end
    end
end

player.CharacterAdded:Connect(function()
    task.wait(0.2)
    setSuperJump()
    setSpeed()
end)

RunService.RenderStepped:Connect(setSuperJump)

-- Auto Attack
local jumpCD = false
RunService.RenderStepped:Connect(function()
    if active.auto and player.Character then
        local hrp = player.Character:FindFirstChild("HumanoidRootPart")
        if hrp then
            local closest, dist = nil, math.huge
            for _, v in pairs(Players:GetPlayers()) do
                if v ~= player and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
                    local d = (v.Character.HumanoidRootPart.Position - hrp.Position).Magnitude
                    if d < dist then dist, closest = d, v end
                end
            end
            if closest and dist < 20 then
                hrp.Velocity = (closest.Character.HumanoidRootPart.Position - hrp.Position).Unit * 60
                if dist < 8 then
                    local tool = player.Character:FindFirstChildOfClass("Tool")
                    if tool then tool:Activate() end
                end
            end
        end
    end
end)

-- Auto Hit
RunService.RenderStepped:Connect(function()
    if active.hit and player.Character then
        local hum = player.Character:FindFirstChildOfClass("Humanoid")
        local hrp = player.Character:FindFirstChild("HumanoidRootPart")
        if hum and hrp then
            local closest, dist = nil, math.huge
            for _, v in pairs(Players:GetPlayers()) do
                if v ~= player and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
                    local d = (v.Character.HumanoidRootPart.Position - hrp.Position).Magnitude
                    if d < dist and d < 10 then dist, closest = d, v end
                end
            end
            if closest then
                hum:MoveTo(closest.Character.HumanoidRootPart.Position)
                local tool = player.Character:FindFirstChildOfClass("Tool")
                if tool then tool:Activate() end
            end
        end
    end
end)

-- Speed
local speedActive = false
local function setSpeed()
    if player.Character then
        local hum = player.Character:FindFirstChildOfClass("Humanoid")
        if hum then
            hum.WalkSpeed = speedActive and 50 or 27
        end
    end
end

spawn(function()
    while true do
        task.wait(0.1)
        if speedActive ~= active.speed then
            speedActive = active.speed
            setSpeed()
        end
        setSpeed()
    end
end)

-- Spin Bot
RunService.RenderStepped:Connect(function()
    if active.spin and player.Character then
        local hrp = player.Character:FindFirstChild("HumanoidRootPart")
        if hrp then
            hrp.CFrame = hrp.CFrame * CFrame.Angles(0, math.rad(25), 0)
        end
    end
end)

-- ESP
RunService.RenderStepped:Connect(function()
    if active.esp then
        for _, v in pairs(Players:GetPlayers()) do
            if v ~= player and v.Character and not v.Character:FindFirstChild("ESP_Box") then
                local box = Instance.new("BoxHandleAdornment")
                box.Name = "ESP_Box"
                box.Size = Vector3.new(4, 5, 4)
                box.Color3 = corAzul
                box.AlwaysOnTop = true
                box.Adornee = v.Character:FindFirstChild("HumanoidRootPart")
                box.Parent = v.Character
            end
        end
    else
        for _, v in pairs(Players:GetPlayers()) do
            if v.Character then
                local box = v.Character:FindFirstChild("ESP_Box")
                if box then box:Destroy() end
            end
        end
    end
end)

-- Abrir/fechar
iconButton.MouseButton1Click:Connect(function()
    menu.Visible = not menu.Visible
end)

fechar.MouseButton1Click:Connect(function()
    menu.Visible = false
end)

print("✅ Zzz V1 Carregado!")
print("📌 Abas: Main | Credits")
print("👨‍💻 Developers: Mathyas and Zzz")