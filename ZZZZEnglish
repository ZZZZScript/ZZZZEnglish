local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TeleportService = game:GetService("TeleportService")

local player = Players.LocalPlayer

-- Remover GUIs antigas
for _, gui in pairs(player.PlayerGui:GetChildren()) do
    if gui.Name == "ZzzUI" then
        gui:Destroy()
    end
end

-- Criar GUI
local gui = Instance.new("ScreenGui")
gui.Name = "ZzzUI"
gui.ResetOnSpawn = false
gui.Parent = player:WaitForChild("PlayerGui")

-- CORES
local corFundo = Color3.fromRGB(8, 8, 10)
local corCard = Color3.fromRGB(20, 20, 25)
local corToggleOff = Color3.fromRGB(45, 45, 55)
local corToggleOn = Color3.fromRGB(100, 100, 120)
local corBolinha = Color3.fromRGB(220, 220, 230)
local corTextoBranco = Color3.fromRGB(255, 255, 255)
local corTextoClaro = Color3.fromRGB(160, 160, 170)
local corDourado = Color3.fromRGB(200, 180, 100)
local corVerde = Color3.fromRGB(100, 180, 100)

-- ========== ÍCONE FLUTUANTE (SIMPLES) ==========
local iconButton = Instance.new("TextButton")
iconButton.Size = UDim2.new(0, 55, 0, 55)
iconButton.Position = UDim2.new(0, 10, 0.5, -27)
iconButton.BackgroundColor3 = Color3.fromRGB(15, 15, 18)
iconButton.Text = "Z"
iconButton.TextColor3 = corTextoBranco
iconButton.TextScaled = true
iconButton.Font = Enum.Font.GothamBlack
iconButton.Parent = gui
Instance.new("UICorner", iconButton).CornerRadius = UDim.new(0, 15)

-- Drag do ícone (SIMPLES)
local dragging = false
local dragStartPos = nil
local startPosition = nil

iconButton.MouseButton1Down:Connect(function()
    dragging = true
    dragStartPos = UserInputService:GetMouseLocation()
    startPosition = iconButton.Position
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = false
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStartPos
        iconButton.Position = UDim2.new(startPosition.X.Scale, startPosition.X.Offset + delta.X, startPosition.Y.Scale, startPosition.Y.Offset + delta.Y)
    end
end)

-- ========== MENU PRINCIPAL ==========
local menu = Instance.new("Frame")
menu.Size = UDim2.new(0, 380, 0, 320)
menu.Position = UDim2.new(0.5, -190, 0.5, -160)
menu.BackgroundColor3 = corFundo
menu.BackgroundTransparency = 0
menu.Visible = false
menu.Parent = gui
Instance.new("UICorner", menu).CornerRadius = UDim.new(0, 12)

-- Drag do menu
local menuDragging = false
local menuDragStartPos = nil
local menuStartPosition = nil

menu.MouseButton1Down:Connect(function()
    menuDragging = true
    menuDragStartPos = UserInputService:GetMouseLocation()
    menuStartPosition = menu.Position
end)

UserInputService.InputChanged:Connect(function(input)
    if menuDragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - menuDragStartPos
        menu.Position = UDim2.new(menuStartPosition.X.Scale, menuStartPosition.X.Offset + delta.X, menuStartPosition.Y.Scale, menuStartPosition.Y.Offset + delta.Y)
    end
end)

-- ========== TOPO ==========
local topo = Instance.new("Frame")
topo.Size = UDim2.new(1, 0, 0, 45)
topo.Position = UDim2.new(0, 0, 0, 0)
topo.BackgroundColor3 = Color3.fromRGB(12, 12, 15)
topo.Parent = menu
Instance.new("UICorner", topo).CornerRadius = UDim.new(0, 12)

local comerciante = Instance.new("TextLabel")
comerciante.Size = UDim2.new(1, 0, 0, 24)
comerciante.Position = UDim2.new(0, 0, 0, 6)
comerciante.Text = "COMERCIANTE"
comerciante.Font = Enum.Font.GothamBlack
comerciante.TextSize = 16
comerciante.TextColor3 = corTextoBranco
comerciante.BackgroundTransparency = 1
comerciante.Parent = topo

local labPvp = Instance.new("TextLabel")
labPvp.Size = UDim2.new(1, 0, 0, 14)
labPvp.Position = UDim2.new(0, 0, 0, 28)
labPvp.Text = "Lab Pvp"
labPvp.Font = Enum.Font.Gotham
labPvp.TextSize = 8
labPvp.TextColor3 = corTextoClaro
labPvp.BackgroundTransparency = 1
labPvp.Parent = topo

local fechar = Instance.new("TextButton")
fechar.Size = UDim2.new(0, 24, 0, 24)
fechar.Position = UDim2.new(1, -30, 0, 6)
fechar.Text = "✕"
fechar.Font = Enum.Font.GothamBold
fechar.TextSize = 12
fechar.TextColor3 = corTextoBranco
fechar.BackgroundTransparency = 1
fechar.Parent = topo

-- ========== ABAS LADO ESQUERDO ==========
local sidebar = Instance.new("Frame")
sidebar.Size = UDim2.new(0, 70, 1, -45)
sidebar.Position = UDim2.new(0, 0, 0, 45)
sidebar.BackgroundColor3 = corCard
sidebar.Parent = menu

local abaMain = Instance.new("TextButton")
abaMain.Size = UDim2.new(0.85, 0, 0, 32)
abaMain.Position = UDim2.new(0.075, 0, 0, 10)
abaMain.Text = "Main"
abaMain.Font = Enum.Font.GothamBold
abaMain.TextSize = 11
abaMain.TextColor3 = corTextoBranco
abaMain.BackgroundColor3 = corToggleOn
abaMain.Parent = sidebar
Instance.new("UICorner", abaMain).CornerRadius = UDim.new(0, 6)

local abaCredits = Instance.new("TextButton")
abaCredits.Size = UDim2.new(0.85, 0, 0, 32)
abaCredits.Position = UDim2.new(0.075, 0, 0, 48)
abaCredits.Text = "Credits"
abaCredits.Font = Enum.Font.GothamBold
abaCredits.TextSize = 11
abaCredits.TextColor3 = corTextoClaro
abaCredits.BackgroundColor3 = corCard
abaCredits.Parent = sidebar
Instance.new("UICorner", abaCredits).CornerRadius = UDim.new(0, 6)

-- ========== CONTEÚDO ==========
local scroll = Instance.new("ScrollingFrame")
scroll.Size = UDim2.new(1, -80, 1, -55)
scroll.Position = UDim2.new(0, 75, 0, 55)
scroll.BackgroundTransparency = 1
scroll.CanvasSize = UDim2.new(0, 0, 0, 320)
scroll.ScrollBarThickness = 3
scroll.ScrollBarImageColor3 = corToggleOn
scroll.Parent = menu

-- ========== FUNÇÃO CRIAR TOGGLE ==========
local function criarToggle(icone, texto, y)
    local card = Instance.new("Frame")
    card.Size = UDim2.new(1, 0, 0, 36)
    card.Position = UDim2.new(0, 0, 0, y)
    card.BackgroundColor3 = corCard
    card.Parent = scroll
    Instance.new("UICorner", card).CornerRadius = UDim.new(0, 6)
    
    local iconeLab = Instance.new("TextLabel")
    iconeLab.Size = UDim2.new(0, 28, 0, 28)
    iconeLab.Position = UDim2.new(0, 6, 0.5, -14)
    iconeLab.Text = icone
    iconeLab.Font = Enum.Font.Gotham
    iconeLab.TextSize = 16
    iconeLab.TextColor3 = corToggleOn
    iconeLab.BackgroundTransparency = 1
    iconeLab.Parent = card
    
    local textoLab = Instance.new("TextLabel")
    textoLab.Size = UDim2.new(0, 100, 0, 20)
    textoLab.Position = UDim2.new(0, 38, 0.5, -10)
    textoLab.Text = texto
    textoLab.Font = Enum.Font.GothamBold
    textoLab.TextSize = 10
    textoLab.TextColor3 = corTextoBranco
    textoLab.BackgroundTransparency = 1
    textoLab.TextXAlignment = Enum.TextXAlignment.Left
    textoLab.Parent = card
    
    local toggle = Instance.new("TextButton")
    toggle.Size = UDim2.new(0, 38, 0, 20)
    toggle.Position = UDim2.new(1, -45, 0.5, -10)
    toggle.Text = ""
    toggle.BackgroundColor3 = corToggleOff
    toggle.Parent = card
    Instance.new("UICorner", toggle).CornerRadius = UDim.new(1, 0)
    
    local bola = Instance.new("Frame")
    bola.Size = UDim2.new(0, 16, 0, 16)
    bola.Position = UDim2.new(0, 2, 0.5, -8)
    bola.BackgroundColor3 = corBolinha
    bola.Parent = toggle
    Instance.new("UICorner", bola).CornerRadius = UDim.new(1, 0)
    
    local ativo = false
    local callback = nil
    
    toggle.MouseButton1Click:Connect(function()
        ativo = not ativo
        if ativo then
            toggle.BackgroundColor3 = corToggleOn
            bola.Position = UDim2.new(1, -18, 0.5, -8)
        else
            toggle.BackgroundColor3 = corToggleOff
            bola.Position = UDim2.new(0, 2, 0.5, -8)
        end
        if callback then callback(ativo) end
    end)
    
    return {
        onToggle = function(cb) callback = cb end,
        getAtivo = function() return ativo end
    }
end

-- ========== CONTEÚDO ==========
local y = 5

local autoAttack = criarToggle("⚔️", "Auto Attack", y)
y = y + 42

local esp = criarToggle("👁️", "ESP", y)
y = y + 42

local spinBot = criarToggle("🌀", "Spin Bot", y)
y = y + 42

local superJump = criarToggle("🦘", "Super Jump", y)
y = y + 42

local autoHit = criarToggle("👊", "Auto Hit", y)
y = y + 42

local autoCollect = criarToggle("💰", "Auto Collect Money", y)
y = y + 50

-- Informações
local infoFrame = Instance.new("Frame")
infoFrame.Size = UDim2.new(1, 0, 0, 70)
infoFrame.Position = UDim2.new(0, 0, 0, y)
infoFrame.BackgroundTransparency = 1
infoFrame.Parent = scroll
y = y + 75

local indice = Instance.new("TextLabel")
indice.Size = UDim2.new(1, 0, 0, 16)
indice.Position = UDim2.new(0, 0, 0, 0)
indice.Text = "📊 Indice"
indice.Font = Enum.Font.GothamBold
indice.TextSize = 10
indice.TextColor3 = corToggleOn
indice.BackgroundTransparency = 1
indice.TextXAlignment = Enum.TextXAlignment.Left
indice.Parent = infoFrame

local duelos = Instance.new("TextLabel")
duelos.Size = UDim2.new(0.5, 0, 0, 14)
duelos.Position = UDim2.new(0, 0, 0, 18)
duelos.Text = "⚔️ Duelos"
duelos.Font = Enum.Font.Gotham
duelos.TextSize = 9
duelos.TextColor3 = corTextoClaro
duelos.BackgroundTransparency = 1
duelos.TextXAlignment = Enum.TextXAlignment.Left
duelos.Parent = infoFrame

local anchor = Instance.new("TextLabel")
anchor.Size = UDim2.new(0.5, 0, 0, 14)
anchor.Position = UDim2.new(0, 0, 0, 34)
anchor.Text = "⚓ Anchor"
anchor.Font = Enum.Font.Gotham
anchor.TextSize = 9
anchor.TextColor3 = corTextoClaro
anchor.BackgroundTransparency = 1
anchor.TextXAlignment = Enum.TextXAlignment.Left
anchor.Parent = infoFrame

local x28 = Instance.new("TextLabel")
x28.Size = UDim2.new(0.3, 0, 0, 18)
x28.Position = UDim2.new(0.5, 0, 0, 18)
x28.Text = "x28"
x28.Font = Enum.Font.GothamBold
x28.TextSize = 14
x28.TextColor3 = corDourado
x28.BackgroundTransparency = 1
x28.Parent = infoFrame

local x30 = Instance.new("TextLabel")
x30.Size = UDim2.new(0.3, 0, 0, 18)
x30.Position = UDim2.new(0.7, 0, 0, 18)
x30.Text = "x30"
x30.Font = Enum.Font.GothamBold
x30.TextSize = 14
x30.TextColor3 = corDourado
x30.BackgroundTransparency = 1
x30.Parent = infoFrame

local money = Instance.new("TextLabel")
money.Size = UDim2.new(0.5, 0, 0, 18)
money.Position = UDim2.new(0, 0, 0, 52)
money.Text = "$684.65T"
money.Font = Enum.Font.GothamBold
money.TextSize = 11
money.TextColor3 = corVerde
money.BackgroundTransparency = 1
money.TextXAlignment = Enum.TextXAlignment.Left
money.Parent = infoFrame

local autoRejoin = Instance.new("TextButton")
autoRejoin.Size = UDim2.new(1, 0, 0, 30)
autoRejoin.Position = UDim2.new(0, 0, 0, y)
autoRejoin.Text = "🔄 Auto Rejoin"
autoRejoin.Font = Enum.Font.GothamBold
autoRejoin.TextSize = 10
autoRejoin.TextColor3 = corTextoBranco
autoRejoin.BackgroundColor3 = corCard
autoRejoin.Parent = scroll
Instance.new("UICorner", autoRejoin).CornerRadius = UDim.new(0, 6)

autoRejoin.MouseButton1Click:Connect(function()
    TeleportService:Teleport(game.PlaceId, player)
end)

y = y + 38
scroll.CanvasSize = UDim2.new(0, 0, 0, y + 10)

-- ========== AUTO COLLECT MONEY (SIMPLES E FUNCIONAL) ==========
local collectActive = false

-- Loop de coleta
spawn(function()
    while true do
        if collectActive then
            local character = player.Character
            if character then
                local hrp = character:FindFirstChild("HumanoidRootPart")
                if hrp then
                    for _, obj in pairs(workspace:GetDescendants()) do
                        if obj:IsA("BasePart") then
                            local nome = obj.Name:lower()
                            if string.find(nome, "coin") or string.find(nome, "money") or string.find(nome, "cash") then
                                pcall(function()
                                    obj.CFrame = hrp.CFrame
                                    obj:Destroy()
                                end)
                            end
                        end
                    end
                end
            end
        end
        task.wait(0.2)
    end
end)

-- ========== ABA CREDITS ==========
local creditsScroll = Instance.new("ScrollingFrame")
creditsScroll.Size = UDim2.new(1, -80, 1, -55)
creditsScroll.Position = UDim2.new(0, 75, 0, 55)
creditsScroll.BackgroundTransparency = 1
creditsScroll.CanvasSize = UDim2.new(0, 0, 0, 140)
creditsScroll.ScrollBarThickness = 3
creditsScroll.ScrollBarImageColor3 = corToggleOn
creditsScroll.Visible = false
creditsScroll.Parent = menu

local creditsY = 5

local creditsTitle = Instance.new("TextLabel")
creditsTitle.Size = UDim2.new(1, 0, 0, 24)
creditsTitle.Position = UDim2.new(0, 0, 0, creditsY)
creditsTitle.Text = "👥 CREDITS"
creditsTitle.Font = Enum.Font.GothamBold
creditsTitle.TextSize = 12
creditsTitle.TextColor3 = corToggleOn
creditsTitle.BackgroundTransparency = 1
creditsTitle.Parent = creditsScroll
creditsY = creditsY + 28

local dev1 = Instance.new("Frame")
dev1.Size = UDim2.new(1, 0, 0, 40)
dev1.Position = UDim2.new(0, 0, 0, creditsY)
dev1.BackgroundColor3 = corCard
dev1.Parent = creditsScroll
Instance.new("UICorner", dev1).CornerRadius = UDim.new(0, 6)

local dev1Name = Instance.new("TextLabel")
dev1Name.Size = UDim2.new(1, 0, 0, 18)
dev1Name.Position = UDim2.new(0, 8, 0, 6)
dev1Name.Text = "👨‍💻 Mathyas - Lead Developer"
dev1Name.Font = Enum.Font.GothamBold
dev1Name.TextSize = 10
dev1Name.TextColor3 = corTextoBranco
dev1Name.BackgroundTransparency = 1
dev1Name.TextXAlignment = Enum.TextXAlignment.Left
dev1Name.Parent = dev1

creditsY = creditsY + 46

local dev2 = Instance.new("Frame")
dev2.Size = UDim2.new(1, 0, 0, 40)
dev2.Position = UDim2.new(0, 0, 0, creditsY)
dev2.BackgroundColor3 = corCard
dev2.Parent = creditsScroll
Instance.new("UICorner", dev2).CornerRadius = UDim.new(0, 6)

local dev2Name = Instance.new("TextLabel")
dev2Name.Size = UDim2.new(1, 0, 0, 18)
dev2Name.Position = UDim2.new(0, 8, 0, 6)
dev2Name.Text = "👨‍💻 Zzz - Co-Developer"
dev2Name.Font = Enum.Font.GothamBold
dev2Name.TextSize = 10
dev2Name.TextColor3 = corTextoBranco
dev2Name.BackgroundTransparency = 1
dev2Name.TextXAlignment = Enum.TextXAlignment.Left
dev2Name.Parent = dev2

creditsY = creditsY + 46

local discordBtn = Instance.new("TextButton")
discordBtn.Size = UDim2.new(1, 0, 0, 32)
discordBtn.Position = UDim2.new(0, 0, 0, creditsY)
discordBtn.Text = "💬 JOIN DISCORD"
discordBtn.Font = Enum.Font.GothamBold
discordBtn.TextSize = 10
discordBtn.TextColor3 = corTextoBranco
discordBtn.BackgroundColor3 = Color3.fromRGB(45, 55, 70)
discordBtn.Parent = creditsScroll
Instance.new("UICorner", discordBtn).CornerRadius = UDim.new(0, 6)

discordBtn.MouseButton1Click:Connect(function()
    setclipboard("https://discord.gg/PDmAh9Ua4")
end)

creditsY = creditsY + 38

local version = Instance.new("TextLabel")
version.Size = UDim2.new(1, 0, 0, 16)
version.Position = UDim2.new(0, 0, 0, creditsY)
version.Text = "Zzz V1 | Premium"
version.Font = Enum.Font.Gotham
version.TextSize = 7
version.TextColor3 = Color3.fromRGB(80, 80, 90)
version.BackgroundTransparency = 1
version.Parent = creditsScroll

creditsScroll.CanvasSize = UDim2.new(0, 0, 0, creditsY + 20)

-- ========== FUNÇÕES ==========
local active = {
    auto = false,
    esp = false,
    spin = false,
    superJump = false,
    hit = false,
    collect = false
}

autoAttack.onToggle(function(v) active.auto = v end)
esp.onToggle(function(v) active.esp = v end)
spinBot.onToggle(function(v) active.spin = v end)
superJump.onToggle(function(v) active.superJump = v end)
autoHit.onToggle(function(v) active.hit = v end)
autoCollect.onToggle(function(v) 
    active.collect = v
    collectActive = v
    print("Auto Collect: " .. (v and "ON" or "OFF"))
end)

-- Super Jump
local function setSuperJump()
    if player.Character then
        local hum = player.Character:FindFirstChildOfClass("Humanoid")
        if hum then
            hum.JumpPower = active.superJump and 120 or 50
        end
    end
end

player.CharacterAdded:Connect(function()
    task.wait(0.2)
    setSuperJump()
end)

RunService.RenderStepped:Connect(setSuperJump)

-- Auto Attack
RunService.RenderStepped:Connect(function()
    if active.auto and player.Character then
        local hrp = player.Character:FindFirstChild("HumanoidRootPart")
        if hrp then
            local closest, dist = nil, math.huge
            for _, v in pairs(Players:GetPlayers()) do
                if v ~= player and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
                    local d = (v.Character.HumanoidRootPart.Position - hrp.Position).Magnitude
                    if d < dist then dist, closest = d, v end
                end
            end
            if closest and dist < 20 then
                hrp.Velocity = (closest.Character.HumanoidRootPart.Position - hrp.Position).Unit * 60
                if dist < 8 then
                    local tool = player.Character:FindFirstChildOfClass("Tool")
                    if tool then tool:Activate() end
                end
            end
        end
    end
end)

-- Auto Hit
RunService.RenderStepped:Connect(function()
    if active.hit and player.Character then
        local hum = player.Character:FindFirstChildOfClass("Humanoid")
        local hrp = player.Character:FindFirstChild("HumanoidRootPart")
        if hum and hrp then
            local closest, dist = nil, math.huge
            for _, v in pairs(Players:GetPlayers()) do
                if v ~= player and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
                    local d = (v.Character.HumanoidRootPart.Position - hrp.Position).Magnitude
                    if d < dist and d < 10 then dist, closest = d, v end
                end
            end
            if closest then
                hum:MoveTo(closest.Character.HumanoidRootPart.Position)
                local tool = player.Character:FindFirstChildOfClass("Tool")
                if tool then tool:Activate() end
            end
        end
    end
end)

-- Spin Bot
RunService.RenderStepped:Connect(function()
    if active.spin and player.Character then
        local hrp = player.Character:FindFirstChild("HumanoidRootPart")
        if hrp then
            hrp.CFrame = hrp.CFrame * CFrame.Angles(0, math.rad(25), 0)
        end
    end
end)

-- ESP
RunService.RenderStepped:Connect(function()
    if active.esp then
        for _, v in pairs(Players:GetPlayers()) do
            if v ~= player and v.Character and not v.Character:FindFirstChild("ESP_Box") then
                local box = Instance.new("BoxHandleAdornment")
                box.Name = "ESP_Box"
                box.Size = Vector3.new(4, 5, 4)
                box.Color3 = corToggleOn
                box.AlwaysOnTop = true
                box.Adornee = v.Character:FindFirstChild("HumanoidRootPart")
                box.Parent = v.Character
            end
        end
    else
        for _, v in pairs(Players:GetPlayers()) do
            if v.Character then
                local box = v.Character:FindFirstChild("ESP_Box")
                if box then box:Destroy() end
            end
        end
    end
end)

-- ========== TROCAR ABA ==========
abaMain.MouseButton1Click:Connect(function()
    abaMain.BackgroundColor3 = corToggleOn
    abaMain.TextColor3 = corTextoBranco
    abaCredits.BackgroundColor3 = corCard
    abaCredits.TextColor3 = corTextoClaro
    scroll.Visible = true
    creditsScroll.Visible = false
end)

abaCredits.MouseButton1Click:Connect(function()
    abaCredits.BackgroundColor3 = corToggleOn
    abaCredits.TextColor3 = corTextoBranco
    abaMain.BackgroundColor3 = corCard
    abaMain.TextColor3 = corTextoClaro
    scroll.Visible = false
    creditsScroll.Visible = true
end)

-- Abrir/fechar
iconButton.MouseButton1Click:Connect(function()
    menu.Visible = not menu.Visible
end)

fechar.MouseButton1Click:Connect(function()
    menu.Visible = false
end)

print("✅ Zzz V1 Carregado!")
print("📌 Ícone Z: Clique e arraste para mover")
print("📌 Menu: Clique na barra superior e arraste")
print("💰 Auto Collect: Coleta dinheiro automaticamente!")