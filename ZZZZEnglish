-- TESTE SIMPLES - EXECUTE ISSO PRIMEIRO
local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local player = Players.LocalPlayer

print("=== INICIANDO TESTE ===")

-- Teste 1: Verificar se o PlayerGui existe
local success, err = pcall(function()
    local playerGui = player:WaitForChild("PlayerGui", 5)
    print("✅ PlayerGui encontrado!")
end)

if not success then
    warn("❌ Erro no PlayerGui: " .. tostring(err))
end

-- Teste 2: Testar a requisição HTTP
local url = "https://discord-node-bot--joaolmadureira2.replit.app/check?id=" .. player.UserId
print("🔗 URL: " .. url)

local success, response = pcall(function()
    return game:HttpGet(url)
end)

if success then
    print("✅ HTTP Request funcionou!")
    print("📥 Resposta: " .. tostring(response))
    
    local success2, data = pcall(function()
        return HttpService:JSONDecode(response)
    end)
    
    if success2 then
        print("✅ JSON decodificado com sucesso!")
        print("📊 Dados: " .. tostring(data))
        if data.access then
            print("🔓 ACESSO LIBERADO!")
        else
            print("🔒 ACESSO NEGADO!")
        end
    else
        print("❌ Erro ao decodificar JSON: " .. tostring(data))
    end
else
    print("❌ Erro no HTTP Request: " .. tostring(response))
end

print("=== TESTE FINALIZADO ===")