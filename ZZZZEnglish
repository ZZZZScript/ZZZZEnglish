local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")

local player = Players.LocalPlayer

-- Variáveis do Fly
local flying = false
local flySpeed = 80

-- Remover GUIs antigas
for _, gui in pairs(player.PlayerGui:GetChildren()) do
    if gui.Name == "PvpLab" or gui.Name == "FunHub" then
        gui:Destroy()
    end
end

-- Criar GUI principal
local gui = Instance.new("ScreenGui")
gui.Name = "PvpLab"
gui.ResetOnSpawn = false
gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
gui.Parent = player:WaitForChild("PlayerGui")

-- CORES (estilo FUNHUB)
local corFundo = Color3.fromRGB(18, 20, 28)
local corCard = Color3.fromRGB(28, 30, 38)
local corAzul = Color3.fromRGB(66, 133, 244)
local corAzulEscuro = Color3.fromRGB(52, 106, 195)
local corTexto = Color3.fromRGB(255, 255, 255)
local corCinza = Color3.fromRGB(150, 155, 170)

-- ========== ÍCONE FLUTUANTE ZZZZ ==========
local iconButton = Instance.new("ImageButton")
iconButton.Size = UDim2.new(0, 55, 0, 55)
iconButton.Position = UDim2.new(0, 15, 0.5, -27)
iconButton.BackgroundColor3 = corAzul
iconButton.BackgroundTransparency = 0
iconButton.Image = "rbxassetid://0"
iconButton.Parent = gui
Instance.new("UICorner", iconButton).CornerRadius = UDim.new(0, 15)

-- Texto do ícone
local iconText = Instance.new("TextLabel")
iconText.Size = UDim2.new(1, 0, 1, 0)
iconText.Text = "ZZZZ"
iconText.Font = Enum.Font.GothamBlack
iconText.TextSize = 18
iconText.TextColor3 = corTexto
iconText.BackgroundTransparency = 1
iconText.Parent = iconButton

-- Efeito de brilho no ícone
local glow = Instance.new("ImageLabel")
glow.Size = UDim2.new(1.3, 0, 1.3, 0)
glow.Position = UDim2.new(-0.15, 0, -0.15, 0)
glow.Image = "rbxassetid://0"
glow.BackgroundTransparency = 1
glow.ImageTransparency = 0.5
glow.Parent = iconButton

-- ========== MENU PRINCIPAL ==========
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 380, 0, 550)
mainFrame.Position = UDim2.new(0.5, -190, 0.5, -275)
mainFrame.BackgroundColor3 = corFundo
mainFrame.BackgroundTransparency = 0
mainFrame.Visible = false
mainFrame.Parent = gui
Instance.new("UICorner", mainFrame).CornerRadius = UDim.new(0, 20)

-- ========== TOPO ==========
local topFrame = Instance.new("Frame")
topFrame.Size = UDim2.new(1, 0, 0, 80)
topFrame.Position = UDim2.new(0, 0, 0, 0)
topFrame.BackgroundColor3 = corAzul
topFrame.BackgroundTransparency = 0
topFrame.Parent = mainFrame
Instance.new("UICorner", topFrame).CornerRadius = UDim.new(0, 20)

-- Título
local titleLabel = Instance.new("TextLabel")
titleLabel.Size = UDim2.new(1, 0, 0, 45)
titleLabel.Position = UDim2.new(0, 0, 0, 20)
titleLabel.Text = "PVP LAB"
titleLabel.Font = Enum.Font.GothamBlack
titleLabel.TextSize = 28
titleLabel.TextColor3 = corTexto
titleLabel.BackgroundTransparency = 1
titleLabel.Parent = topFrame

-- Subtítulo
local subLabel = Instance.new("TextLabel")
subLabel.Size = UDim2.new(1, 0, 0, 20)
subLabel.Position = UDim2.new(0, 0, 0, 58)
subLabel.Text = "PREMIUM SCRIPT"
subLabel.Font = Enum.Font.Gotham
subLabel.TextSize = 10
subLabel.TextColor3 = corTexto
subLabel.TextTransparency = 0.4
subLabel.BackgroundTransparency = 1
subLabel.Parent = topFrame

-- ========== CONTEÚDO ==========
local scrollFrame = Instance.new("ScrollingFrame")
scrollFrame.Size = UDim2.new(1, -20, 1, -100)
scrollFrame.Position = UDim2.new(0, 10, 0, 90)
scrollFrame.BackgroundTransparency = 1
scrollFrame.CanvasSize = UDim2.new(0, 0, 0, 650)
scrollFrame.ScrollBarThickness = 3
scrollFrame.ScrollBarImageColor3 = corAzul
scrollFrame.Parent = mainFrame

-- ========== FUNÇÃO CRIAR CARD ==========
local function criarCard(icone, titulo, desc, yPos, corIcone)
    local card = Instance.new("Frame")
    card.Size = UDim2.new(1, 0, 0, 65)
    card.Position = UDim2.new(0, 0, 0, yPos)
    card.BackgroundColor3 = corCard
    card.BackgroundTransparency = 0
    card.Parent = scrollFrame
    Instance.new("UICorner", card).CornerRadius = UDim.new(0, 12)
    
    -- Ícone
    local iconLabel = Instance.new("TextLabel")
    iconLabel.Size = UDim2.new(0, 45, 0, 45)
    iconLabel.Position = UDim2.new(0, 12, 0.5, -22.5)
    iconLabel.Text = icone
    iconLabel.Font = Enum.Font.Gotham
    iconLabel.TextSize = 28
    iconLabel.TextColor3 = corIcone or corAzul
    iconLabel.BackgroundTransparency = 1
    iconLabel.Parent = card
    
    -- Título
    local titleCard = Instance.new("TextLabel")
    titleCard.Size = UDim2.new(0, 180, 0, 22)
    titleCard.Position = UDim2.new(0, 65, 0, 12)
    titleCard.Text = titulo
    titleCard.Font = Enum.Font.GothamBold
    titleCard.TextSize = 14
    titleCard.TextColor3 = corTexto
    titleCard.BackgroundTransparency = 1
    titleCard.TextXAlignment = Enum.TextXAlignment.Left
    titleCard.Parent = card
    
    -- Descrição
    local descCard = Instance.new("TextLabel")
    descCard.Size = UDim2.new(0, 200, 0, 18)
    descCard.Position = UDim2.new(0, 65, 0, 35)
    descCard.Text = desc
    descCard.Font = Enum.Font.Gotham
    descCard.TextSize = 9
    descCard.TextColor3 = corCinza
    descCard.BackgroundTransparency = 1
    descCard.TextXAlignment = Enum.TextXAlignment.Left
    descCard.Parent = card
    
    -- Botão toggle
    local toggleBtn = Instance.new("TextButton")
    toggleBtn.Size = UDim2.new(0, 52, 0, 28)
    toggleBtn.Position = UDim2.new(1, -65, 0.5, -14)
    toggleBtn.Text = ""
    toggleBtn.BackgroundColor3 = Color3.fromRGB(55, 58, 70)
    toggleBtn.Parent = card
    Instance.new("UICorner", toggleBtn).CornerRadius = UDim.new(1, 0)
    
    -- Círculo
    local circle = Instance.new("Frame")
    circle.Size = UDim2.new(0, 22, 0, 22)
    circle.Position = UDim2.new(0, 4, 0.5, -11)
    circle.BackgroundColor3 = corTexto
    circle.Parent = toggleBtn
    Instance.new("UICorner", circle).CornerRadius = UDim.new(1, 0)
    
    -- Status
    local statusLabel = Instance.new("TextLabel")
    statusLabel.Size = UDim2.new(0, 40, 0, 20)
    statusLabel.Position = UDim2.new(1, -120, 0.5, -10)
    statusLabel.Text = "OFF"
    statusLabel.Font = Enum.Font.GothamBold
    statusLabel.TextSize = 10
    statusLabel.TextColor3 = Color3.fromRGB(255, 80, 80)
    statusLabel.BackgroundTransparency = 1
    statusLabel.Parent = card
    
    local ativo = false
    
    local function setAtivo(val)
        ativo = val
        if ativo then
            toggleBtn.BackgroundColor3 = corAzul
            circle.Position = UDim2.new(1, -26, 0.5, -11)
            statusLabel.Text = "ON"
            statusLabel.TextColor3 = Color3.fromRGB(80, 255, 80)
        else
            toggleBtn.BackgroundColor3 = Color3.fromRGB(55, 58, 70)
            circle.Position = UDim2.new(0, 4, 0.5, -11)
            statusLabel.Text = "OFF"
            statusLabel.TextColor3 = Color3.fromRGB(255, 80, 80)
        end
        if callback then callback(ativo) end
    end
    
    local callback = nil
    
    toggleBtn.MouseButton1Click:Connect(function()
        setAtivo(not ativo)
    end)
    
    return {
        setAtivo = setAtivo,
        getAtivo = function() return ativo end,
        onToggle = function(cb) callback = cb end
    }
end

-- ========== CRIAR CARDS ==========
local yPos = 0
local funcoes = {}

-- Seção PVP
local pvpSection = Instance.new("TextLabel")
pvpSection.Size = UDim2.new(1, 0, 0, 32)
pvpSection.Position = UDim2.new(0, 0, 0, yPos)
pvpSection.Text = "⚔️ PVP FUNCTIONS"
pvpSection.Font = Enum.Font.GothamBold
pvpSection.TextSize = 14
pvpSection.TextColor3 = corAzul
pvpSection.BackgroundTransparency = 1
pvpSection.TextXAlignment = Enum.TextXAlignment.Left
pvpSection.Parent = scrollFrame
yPos = yPos + 38

funcoes.auto = criarCard("⚔️", "Auto Attack", "Automatically attack enemies", yPos, corAzul)
yPos = yPos + 70

funcoes.esp = criarCard("👁️", "ESP", "See enemies through walls", yPos, corAzul)
yPos = yPos + 70

funcoes.spin = criarCard("🌀", "Spin Bot", "Auto spin while moving", yPos, corAzul)
yPos = yPos + 70

funcoes.reset = criarCard("💀", "Reset Player", "Auto reset when low health", yPos, corAzul)
yPos = yPos + 70

funcoes.jump = criarCard("🦘", "Super Jump", "Jump higher than normal", yPos, corAzul)
yPos = yPos + 70

funcoes.hit = criarCard("👊", "Auto Hit", "Auto attack nearby enemies", yPos, corAzul)
yPos = yPos + 70

funcoes.speed = criarCard("🏃", "Speed", "Increase movement speed", yPos, corAzul)
yPos = yPos + 80

-- Seção FLY
local flySection = Instance.new("TextLabel")
flySection.Size = UDim2.new(1, 0, 0, 32)
flySection.Position = UDim2.new(0, 0, 0, yPos)
flySection.Text = "🕊️ FLY SYSTEM"
flySection.Font = Enum.Font.GothamBold
flySection.TextSize = 14
flySection.TextColor3 = corAzul
flySection.BackgroundTransparency = 1
flySection.TextXAlignment = Enum.TextXAlignment.Left
flySection.Parent = scrollFrame
yPos = yPos + 38

-- Card Fly
local flyCard = Instance.new("Frame")
flyCard.Size = UDim2.new(1, 0, 0, 85)
flyCard.Position = UDim2.new(0, 0, 0, yPos)
flyCard.BackgroundColor3 = corCard
flyCard.Parent = scrollFrame
Instance.new("UICorner", flyCard).CornerRadius = UDim.new(0, 12)

-- Ícone Fly
local flyIcon = Instance.new("TextLabel")
flyIcon.Size = UDim2.new(0, 50, 0, 50)
flyIcon.Position = UDim2.new(0, 15, 0.5, -25)
flyIcon.Text = "🕊️"
flyIcon.Font = Enum.Font.Gotham
flyIcon.TextSize = 32
flyIcon.TextColor3 = corAzul
flyIcon.BackgroundTransparency = 1
flyIcon.Parent = flyCard

-- Título Fly
local flyTitleCard = Instance.new("TextLabel")
flyTitleCard.Size = UDim2.new(0, 150, 0, 25)
flyTitleCard.Position = UDim2.new(0, 75, 0, 15)
flyTitleCard.Text = "FLY MODE"
flyTitleCard.Font = Enum.Font.GothamBold
flyTitleCard.TextSize = 15
flyTitleCard.TextColor3 = corTexto
flyTitleCard.BackgroundTransparency = 1
flyTitleCard.TextXAlignment = Enum.TextXAlignment.Left
flyTitleCard.Parent = flyCard

-- Descrição Fly
local flyDesc = Instance.new("TextLabel")
flyDesc.Size = UDim2.new(0, 200, 0, 18)
flyDesc.Position = UDim2.new(0, 75, 0, 38)
flyDesc.Text = "Use the joystick to fly"
flyDesc.Font = Enum.Font.Gotham
flyDesc.TextSize = 9
flyDesc.TextColor3 = corCinza
flyDesc.BackgroundTransparency = 1
flyDesc.TextXAlignment = Enum.TextXAlignment.Left
flyDesc.Parent = flyCard

-- Botão Fly Toggle
local flyToggle = Instance.new("TextButton")
flyToggle.Size = UDim2.new(0, 58, 0, 32)
flyToggle.Position = UDim2.new(1, -75, 0.5, -16)
flyToggle.Text = ""
flyToggle.BackgroundColor3 = Color3.fromRGB(55, 58, 70)
flyToggle.Parent = flyCard
Instance.new("UICorner", flyToggle).CornerRadius = UDim.new(1, 0)

local flyCircle = Instance.new("Frame")
flyCircle.Size = UDim2.new(0, 26, 0, 26)
flyCircle.Position = UDim2.new(0, 4, 0.5, -13)
flyCircle.BackgroundColor3 = corTexto
flyCircle.Parent = flyToggle
Instance.new("UICorner", flyCircle).CornerRadius = UDim.new(1, 0)

-- Status Fly
local flyStatus = Instance.new("TextLabel")
flyStatus.Size = UDim2.new(0, 45, 0, 22)
flyStatus.Position = UDim2.new(1, -145, 0.5, -11)
flyStatus.Text = "OFF"
flyStatus.Font = Enum.Font.GothamBold
flyStatus.TextSize = 11
flyStatus.TextColor3 = Color3.fromRGB(255, 80, 80)
flyStatus.BackgroundTransparency = 1
flyStatus.Parent = flyCard

-- Botão Velocidade
local speedBtn = Instance.new("TextButton")
speedBtn.Size = UDim2.new(0, 65, 0, 32)
speedBtn.Position = UDim2.new(1, -225, 0.5, -16)
speedBtn.Text = tostring(flySpeed)
speedBtn.Font = Enum.Font.GothamBold
speedBtn.TextSize = 14
speedBtn.TextColor3 = corTexto
speedBtn.BackgroundColor3 = Color3.fromRGB(45, 48, 60)
speedBtn.Parent = flyCard
Instance.new("UICorner", speedBtn).CornerRadius = UDim.new(0, 8)

yPos = yPos + 95

-- Seção SOCIAL
local socialSection = Instance.new("TextLabel")
socialSection.Size = UDim2.new(1, 0, 0, 32)
socialSection.Position = UDim2.new(0, 0, 0, yPos)
socialSection.Text = "💬 SOCIAL"
socialSection.Font = Enum.Font.GothamBold
socialSection.TextSize = 14
socialSection.TextColor3 = corAzul
socialSection.BackgroundTransparency = 1
socialSection.TextXAlignment = Enum.TextXAlignment.Left
socialSection.Parent = scrollFrame
yPos = yPos + 38

-- Card Discord
local discordCard = Instance.new("Frame")
discordCard.Size = UDim2.new(1, 0, 0, 60)
discordCard.Position = UDim2.new(0, 0, 0, yPos)
discordCard.BackgroundColor3 = corCard
discordCard.Parent = scrollFrame
Instance.new("UICorner", discordCard).CornerRadius = UDim.new(0, 12)

local discordIconLabel = Instance.new("TextLabel")
discordIconLabel.Size = UDim2.new(0, 45, 0, 45)
discordIconLabel.Position = UDim2.new(0, 12, 0.5, -22.5)
discordIconLabel.Text = "💬"
discordIconLabel.Font = Enum.Font.Gotham
discordIconLabel.TextSize = 28
discordIconLabel.TextColor3 = Color3.fromRGB(88, 101, 242)
discordIconLabel.BackgroundTransparency = 1
discordIconLabel.Parent = discordCard

local discordTitle = Instance.new("TextLabel")
discordTitle.Size = UDim2.new(0, 150, 0, 22)
discordTitle.Position = UDim2.new(0, 65, 0, 12)
discordTitle.Text = "JOIN DISCORD"
discordTitle.Font = Enum.Font.GothamBold
discordTitle.TextSize = 14
discordTitle.TextColor3 = corTexto
discordTitle.BackgroundTransparency = 1
discordTitle.TextXAlignment = Enum.TextXAlignment.Left
discordTitle.Parent = discordCard

local discordDesc = Instance.new("TextLabel")
discordDesc.Size = UDim2.new(0, 200, 0, 18)
discordDesc.Position = UDim2.new(0, 65, 0, 35)
discordDesc.Text = "Click to copy Discord link"
discordDesc.Font = Enum.Font.Gotham
discordDesc.TextSize = 9
discordDesc.TextColor3 = corCinza
discordDesc.BackgroundTransparency = 1
discordDesc.TextXAlignment = Enum.TextXAlignment.Left
discordDesc.Parent = discordCard

local discordBtn = Instance.new("TextButton")
discordBtn.Size = UDim2.new(1, 0, 1, 0)
discordBtn.BackgroundTransparency = 1
discordBtn.Text = ""
discordBtn.Parent = discordCard

discordBtn.MouseButton1Click:Connect(function()
    setclipboard("https://discord.gg/PDmAh9Ua4")
    local notif = Instance.new("TextLabel")
    notif.Size = UDim2.new(0, 220, 0, 32)
    notif.Position = UDim2.new(0.5, -110, 0.5, 200)
    notif.Text = "✓ Discord link copied!"
    notif.BackgroundColor3 = Color3.fromRGB(0, 170, 0)
    notif.TextColor3 = corTexto
    notif.Font = Enum.Font.GothamBold
    notif.TextSize = 12
    notif.Parent = mainFrame
    Instance.new("UICorner", notif).CornerRadius = UDim.new(0, 8)
    task.wait(2)
    notif:Destroy()
end)

yPos = yPos + 70

-- Versão
local versionLabel = Instance.new("TextLabel")
versionLabel.Size = UDim2.new(1, 0, 0, 25)
versionLabel.Position = UDim2.new(0, 0, 0, yPos)
versionLabel.Text = "VERSION 3.0 | POWERED BY PVP LAB"
versionLabel.Font = Enum.Font.Gotham
versionLabel.TextSize = 8
versionLabel.TextColor3 = Color3.fromRGB(80, 85, 100)
versionLabel.BackgroundTransparency = 1
versionLabel.Parent = scrollFrame

yPos = yPos + 35
scrollFrame.CanvasSize = UDim2.new(0, 0, 0, yPos)

-- ========== FUNÇÃO FLY ==========
local flyingActive = false
local bv = nil

local function startFly()
    if flyingActive then return end
    local character = player.Character
    if not character then return end
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    local hrp = character:FindFirstChild("HumanoidRootPart")
    if not humanoid or not hrp then return end
    
    flyingActive = true
    flyToggle.BackgroundColor3 = corAzul
    flyCircle.Position = UDim2.new(1, -28, 0.5, -13)
    flyStatus.Text = "ON"
    flyStatus.TextColor3 = Color3.fromRGB(80, 255, 80)
    
    humanoid.PlatformStand = true
    bv = Instance.new("BodyVelocity")
    bv.MaxForce = Vector3.new(1e9, 1e9, 1e9)
    bv.Velocity = Vector3.new(0, 0, 0)
    bv.Parent = hrp
end

local function stopFly()
    if not flyingActive then return end
    flyingActive = false
    flyToggle.BackgroundColor3 = Color3.fromRGB(55, 58, 70)
    flyCircle.Position = UDim2.new(0, 4, 0.5, -13)
    flyStatus.Text = "OFF"
    flyStatus.TextColor3 = Color3.fromRGB(255, 80, 80)
    
    local character = player.Character
    if character then
        local humanoid = character:FindFirstChildOfClass("Humanoid")
        if humanoid then humanoid.PlatformStand = false end
        if bv then bv:Destroy() end
        bv = nil
    end
end

flyToggle.MouseButton1Click:Connect(function()
    if flyingActive then stopFly() else startFly() end
end)

speedBtn.MouseButton1Click:Connect(function()
    flySpeed = flySpeed + 10
    if flySpeed > 200 then flySpeed = 20 end
    speedBtn.Text = tostring(flySpeed)
end)

-- Movimento do Fly
RunService.RenderStepped:Connect(function()
    if not flyingActive then return end
    local character = player.Character
    if not character then return end
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    local hrp = character:FindFirstChild("HumanoidRootPart")
    if not hrp or not bv then return end
    if humanoid then humanoid.PlatformStand = true end
    
    local moveDir = humanoid.MoveDirection
    local camera = workspace.CurrentCamera
    
    if moveDir.Magnitude > 0 then
        local forward = camera.CFrame.LookVector
        local right = camera.CFrame.RightVector
        local moveVector = (forward * moveDir.Z + right * moveDir.X) * flySpeed
        bv.Velocity = moveVector
    else
        bv.Velocity = Vector3.new(0, 0, 0)
    end
end)

-- ========== PVP FUNCTIONS ==========
local activeFunctions = {
    auto = false,
    esp = false,
    spin = false,
    reset = false,
    jump = false,
    hit = false,
    speed = false
}

funcoes.auto.onToggle(function(v) activeFunctions.auto = v end)
funcoes.esp.onToggle(function(v) activeFunctions.esp = v end)
funcoes.spin.onToggle(function(v) activeFunctions.spin = v end)
funcoes.reset.onToggle(function(v) activeFunctions.reset = v end)
funcoes.jump.onToggle(function(v) activeFunctions.jump = v end)
funcoes.hit.onToggle(function(v) activeFunctions.hit = v end)
funcoes.speed.onToggle(function(v) activeFunctions.speed = v end)

-- Auto Attack
RunService.RenderStepped:Connect(function()
    if activeFunctions.auto and player.Character and not flyingActive then
        local hrp = player.Character:FindFirstChild("HumanoidRootPart")
        if hrp then
            local closest, dist = nil, math.huge
            for _, v in pairs(Players:GetPlayers()) do
                if v ~= player and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
                    local d = (v.Character.HumanoidRootPart.Position - hrp.Position).Magnitude
                    if d < dist then dist, closest = d, v end
                end
            end
            if closest then
                hrp.Velocity = (closest.Character.HumanoidRootPart.Position - hrp.Position).Unit * 60
            end
        end
    end
end)

-- Auto Hit
RunService.RenderStepped:Connect(function()
    if activeFunctions.hit and player.Character and not flyingActive then
        local hum = player.Character:FindFirstChildOfClass("Humanoid")
        local hrp = player.Character:FindFirstChild("HumanoidRootPart")
        if hum and hrp then
            local closest, dist = nil, math.huge
            for _, v in pairs(Players:GetPlayers()) do
                if v ~= player and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
                    local d = (v.Character.HumanoidRootPart.Position - hrp.Position).Magnitude
                    if d < dist and d < 10 then dist, closest = d, v end
                end
            end
            if closest and closest.Character then
                hum:MoveTo(closest.Character.HumanoidRootPart.Position)
                local tool = player.Character:FindFirstChildOfClass("Tool")
                if tool then tool:Activate() end
            end
        end
    end
end)

-- Speed
local speedActiveFlag = false
local function setSpeed()
    if player.Character then
        local hum = player.Character:FindFirstChildOfClass("Humanoid")
        if hum and not flyingActive then
            hum.WalkSpeed = speedActiveFlag and 50 or 27
        end
    end
end

spawn(function()
    while true do
        task.wait(0.1)
        if speedActiveFlag ~= activeFunctions.speed then
            speedActiveFlag = activeFunctions.speed
            setSpeed()
        end
        setSpeed()
    end
end)

player.CharacterAdded:Connect(function()
    task.wait(0.2)
    setSpeed()
end)

-- Spin Bot
RunService.RenderStepped:Connect(function()
    if activeFunctions.spin and player.Character and not flyingActive then
        local hrp = player.Character:FindFirstChild("HumanoidRootPart")
        if hrp then hrp.CFrame = hrp.CFrame * CFrame.Angles(0, math.rad(25), 0) end
    end
end)

-- Reset Player
local resetCooldown = false
RunService.RenderStepped:Connect(function()
    if activeFunctions.reset and not resetCooldown then
        resetCooldown = true
        local hum = player.Character and player.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum.Health = 0 end
        task.wait(1)
        resetCooldown = false
    end
end)

-- Super Jump
local jumpTime = 0
UserInputService.InputBegan:Connect(function(input)
    if activeFunctions.jump and input.KeyCode == Enum.KeyCode.Space and not flyingActive then
        local hum = player.Character and player.Character:FindFirstChildOfClass("Humanoid")
        if hum and tick() - jumpTime > 0.1 then
            jumpTime = tick()
            hum:ChangeState(Enum.HumanoidStateType.Jumping)
            hum.JumpPower = 120
        end
    end
end)

RunService.RenderStepped:Connect(function()
    if player.Character and not flyingActive then
        local hum = player.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum.JumpPower = activeFunctions.jump and 120 or 50 end
    end
end)

-- ESP
RunService.RenderStepped:Connect(function()
    if activeFunctions.esp then
        for _, v in pairs(Players:GetPlayers()) do
            if v ~= player and v.Character and not v.Character:FindFirstChild("ESP_Box") then
                local box = Instance.new("BoxHandleAdornment")
                box.Name = "ESP_Box"
                box.Size = Vector3.new(4, 5, 4)
                box.Color3 = corAzul
                box.AlwaysOnTop = true
                box.Adornee = v.Character:FindFirstChild("HumanoidRootPart")
                box.Parent = v.Character
            end
        end
    else
        for _, v in pairs(Players:GetPlayers()) do
            if v.Character then
                local box = v.Character:FindFirstChild("ESP_Box")
                if box then box:Destroy() end
            end
        end
    end
end)

-- ========== ABRIR/FECHAR MENU ==========
local menuAberto = false

iconButton.MouseButton1Click:Connect(function()
    menuAberto = not menuAberto
    mainFrame.Visible = menuAberto
    
    -- Animação
    if menuAberto then
        mainFrame:TweenPosition(UDim2.new(0.5, -190, 0.5, -275), "Out", "Quad", 0.3, true)
    end
end)

closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 30, 0, 30)
closeBtn.Position = UDim2.new(1, -40, 0, 10)
closeBtn.Text = "✕"
closeBtn.Font = Enum.Font.GothamBold
closeBtn.TextSize = 18
closeBtn.TextColor3 = corTexto
closeBtn.BackgroundTransparency = 1
closeBtn.Parent = mainFrame

closeBtn.MouseButton1Click:Connect(function()
    menuAberto = false
    mainFrame.Visible = false
end)

-- Limpeza
player.CharacterRemoving:Connect(stopFly)

print("✅ PVP LAB CARREGADO!")
print("📌 Ícone ZZZZ no canto esquerdo da tela")
print("🕊️ Ative o FLY e use o joystick para voar!")